import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.substitutemanagment.com',
  appName: 'Substitue Managment',
  webDir: 'dist'
};

export default config;
